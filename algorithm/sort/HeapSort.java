package applicationOfDataStructure;

import dataStrucrure.PriorityQueue;

/**
 * using priority queue to implement heapSort algorithm
 * @author 12475
 *
 */

public class HeapSort<key extends Comparable<key>> {
    private key[] arr;
    public void heapSort(key[] arr){
        PriorityQueue pq = new PriorityQueue(arr);
        
    }
}
